	<%if current_user.subscribed_books.exists?(@book.id)%>
		$(".subs a").on('ajax:success', function(e) {
		e.preventDefault();
        $(this).html("Unsubscribe")
});	  
	<%else%>
		$(".subs a").on('ajax:success', function(e) { 
		e.preventDefault();
        $(this).html("Subscribe")
});
	<%end%>
	console.log("hihi")
